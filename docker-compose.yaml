services:
  rag-mcp:
    build: .
    ports:
      - "${EXTERNAL_PORT:-8001}:8000"
    volumes:
      - ${DATA_DIR}:/data
      - rag-store:/store
    environment:
      - BASE_URL=http://localhost:${EXTERNAL_PORT:-8001}

volumes:
  rag-store:
