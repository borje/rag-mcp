services:
  rag-mcp:
    build: .
    ports:
      - "${EXTERNAL_PORT:-8001}:8000"
    volumes:
      - ${DATA_DIR}:/data
      - rag-store:/store
      - rag-models:/models
    environment:
      - BASE_URL=http://${FQDN:-localhost}:${EXTERNAL_PORT:-8001}

volumes:
  rag-store:
  rag-models:
